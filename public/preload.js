// const { ipcMain } = require("electron");
// const { GetSourcesSync, GetSourceSync } = require('./sources');

// ipcMain.handle('get-markdown-path', async (e, ...args) => {
//     return await GetSourcesSync();
// });

window.addEventListener('DOMContentLoaded', () => {
    // window.__api__.getSourceSync = GetSourceSync;
})
